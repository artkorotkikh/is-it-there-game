---
title: Status and change log
tags: [status, verification]
---

# Status and change log

Last updated: 2026-09-06. **0.8.0** frontend and rooms are published on the owner's Cloud Engine; records is **0.6.1** and the separate [statistics panel](https://zhtrs-oiaaa-aaabz-aadga-cai.icp.net/) is **0.2.0**. Co-op collection is live and the existing panel reader’s rooms grant is confirmed. [Play](https://isitthereyet.nano-tema--0v1.opencloud.org/). Expeditions use direct connections; TURN/separate-network verification remains outstanding. Deployment IDs/hashes and controller commands are in [Deployment](./deployment.md); current behavior is in [Project](./project.md) and [Tracks, profiles and leaderboard](./solo-records.md). Earlier entries describe their pre-release state.

## Co-op analytics 0.8.0 — published; reader access confirmed, 2026-09-06

Added persistent room-created/guest-joined/connected-pair/first-start counters to rooms, a bounded best-effort client reporter, and four dashboard cards with a separate collection date and co-op CSV. Both participants must report connection/start; retries and restarts do not inflate totals. Sessions report for six hours with a 5,000-entry bound; lifetime totals survive expiry/eviction/upgrades. Reader grants are independent of records. No prior activity is reconstructed. Profiles/records code and state are untouched.

Automatically verified: rooms Wasm build, strict TypeScript and frontend/rooms/admin production builds; 14 focused client tests; 11 rooms/backend cases, including a 5,001-room capacity run proving eviction preserves lifetime totals and does not block creation, and actual pre-analytics Wasm upgrade retaining a live invite, member/reader isolation, deduplication, expiry and grant preservation. One signed two-account Playwright journey passed: real local WebRTC, all four counters become one, restart stays one, dashboard CSV, revoke/regrant, logout, zero dashboard update calls and no page errors. Desktop/mobile screenshots were visually inspected; 390 px layout has no horizontal overflow. Human II gesture is substituted by the test identity fixture; separate-network/TURN validation is not part of this analytics check.

Published existing rooms/frontend/admin in place; metadata confirms 0.8.0/0.8.0/0.2.0. Public read-only smoke passed exact game/panel assets, headers, sibling service IDs, records 0.6.1 and anonymous denial. Controller query confirms the new counters and collection timestamp. Records ID, module hash, settings and memory match pre-deployment readback; no records deployment or synthetic production events. The user's earlier co-op startup confirmation remains the real-account gameplay evidence.

Initially blocked (resolved below): automatic approval review rejected mirroring the existing records reader into rooms, including a retry supported by a fresh controller-authorized `listStatisticsReaders` query. It requires explicit user approval for that precise production ACL grant. No grant was applied; the panel keeps solo access and shows co-op access unavailable until approved. The controller can read co-op totals already. Reload both game tabs and create a new room for telemetry; existing pre-upgrade rooms and earlier sessions are not backfilled.

The user explicitly approved the named account and requested redeployment. Applied its rooms reader grant successfully; controller readback confirms the exact account. Redeployed frontend/rooms/admin at the same versions; frontend's 13 and admin's five assets were already current. Both public read-only smoke checks passed again. All four services are Running with unchanged module hashes; records ID/settings are unchanged and records was not deployed (memory changed between observations, so no identical-memory claim is made). Co-op collection timestamp survived and all counters currently read zero. No synthetic events were submitted. Prior gameplay tests remain applicable because no gameplay code changed; documentation checks passed. The user's authenticated view after this grant has not been observed.

## Post-deploy co-op version rejection — 2026-09-06

The user reported the published “different map version” rejection after 0.7.1. Code inspection confirms this branch compares the invitation's full map/tuning/interaction key before creating a peer; it is not an ICE/TURN failure. 0.7.0 and 0.7.1 use different interaction suffixes (net2/net3), so an older open host tab can produce an incompatible invitation. This is the likely explanation, not a verified reading of the user's two tab versions. The shown content-script/TronLink/polkadot warnings appear to be extension output, not the game's version check.

Clarified both mismatch messages locally: reload both players, then have the host create a new expedition and share its new invite. Reusing the old invitation after reloading is insufficient. Compatibility checks remain intact; no automatic reload, room creation, profile changes or deployment. This is a copy-only correction; existing compatibility tests remain applicable and no complete browser game was replayed. Strict TypeScript/production build passed with the existing bundle warnings; documentation validation passed with 15 entrypoints. The user subsequently confirmed that co-op starts successfully after retrying. The clarified copy ships with 0.8.0.

## Interaction fix 0.7.1 and multiplayer analytics audit — published, 2026-09-06

The user requested redeployment and a check of multiplayer measurement. Published only the existing frontend with the previously verified crowded-item selection fix: item outline/name, X/touch cycling, accessible cargo priority and exact target validation. Records 0.6.1, rooms 0.7.0 and admin 0.1.1 were not targeted; their IDs, module hashes, full settings and memory are unchanged before/after. The linked owner identity remained valid. No records reinstall, profile/grant edits or synthetic production events.

Verified: 51 focused physical/interaction/protocol tests passed, deployment build succeeded, frontend metadata is 0.7.1 and thirteen assets synced. Public read-only checks passed exact served assets, headers and frontend/records/rooms wiring, records version and anonymous access boundaries. Chrome public guest expedition startup, walking and pause/resume passed with zero production canister update calls. Prior representative keyboard/touch selection evidence remains applicable; it was not replayed. Documentation validation passed with 15 entrypoints. [Deployment](./deployment.md) records hashes and command. Both teammates need a reload and fresh invitation (net3).

Analytics audit: no historical multiplayer events/counters currently exist in the expedition client, rooms service or panel API. The rooms service retains only temporary member/signaling state, removed on expiry/replacement/close. The panel reads existing solo aggregates. Created rooms, joining accounts, successful peer connections and expedition starts need separate collection; existing data cannot supply historical totals or unique people. The [measurement plan](./multiplayer-accounts.md) remains planned, and this release adds no telemetry. Direct-only/TURN and separate-network limitations are unchanged.

## English statistics panel — 2026-09-06

The owner confirmed the live authenticated panel works. Changed the panel's default interface to English (0.1.1): headings, instructions, errors, table labels, page language/title and English number/date formatting. CSV field names were already English and its raw counts/UTC timestamps are unchanged. No language selector was requested or added. Records API, access grants and collection behavior are unchanged. Verified: production build with strict TypeScript, two existing calculation/CSV tests updated for decimal-point formatting, and documentation validation passed. Published only admin 0.1.1 (five assets); public read-only checks passed exact English HTML/assets, headers, canister wiring and anonymous denial. No backend redeployment, new telemetry or permission edits. Existing browser/security evidence was reused for this copy/formatting-only change; no full browser flow was replayed.

## Separate statistics panel — published, 2026-09-06

Implemented a standalone Russian-language `admin` static-site build and records 0.6.1 reader API. The panel displays existing opens/starts/finishes/saves, per-track/rules-version rows, event ratios, collection/fetch times and CSV. It uses a separate II origin; no game profile/event writes or co-op telemetry are added. Controller-only bounded reader grants survive upgrade and are checked against their issuing controller's current status. Existing controller statistics and private player APIs retain their access boundaries. See [Admin](./admin.md).

Verified locally: admin production build and strict TypeScript, plus `npm exec -- icp build records admin -e ic` for both deployment artifacts; two calculation/CSV tests; 28 actual-Wasm backend cases in total (25 existing records/profile/delivery/rooms cases plus three new admin cases), including the archived 0.6.0 upgrade preserving counters, profile, records, retry receipt and pending delivery. The initial new upgrade tests omitted the required enhanced-persistence option; the corrected three-case run passed. One Chrome scenario passed in 24.3 seconds against a signed disposable backend: sign-in denial/grant, empty and populated totals, CSV, retry, session restoration, access revocation, session expiry and logout with an in-flight reply. Desktop and 390 px screenshots were inspected. No browser update calls were made. Fixtures seed test counters; the human II gesture is substituted and no new physical-gameplay claim is made. Initial sandbox port restrictions and a local Vite backend-cookie collision were isolated and corrected in the test setup. Documentation validation passed with 15 entrypoints.

Published after the owner renewed the existing Cloud Engine delegation: new admin `zhtrs-oiaaa-aaabz-aadga-cai` (0.1.0), existing records upgraded in place to 0.6.1. Only `records admin` were deployed; frontend/rooms were not targeted. Five admin assets synced. Both canisters are Running with the existing controller, and module hashes match build artifacts. Public read-only checks passed exact HTML/JS/CSS/II metadata, headers, records/admin wiring, records version and anonymous rejection for panel statistics, grants and private player APIs. The controller statistics response is byte-identical before/after: 19 opens, 13 starts, 3 finishes and 1 save; collection timestamp retained. No synthetic production events, profiles or results were created.

The owner supplied the panel-origin II principal after sign-in. The controller granted read-only statistics access; `setStatisticsReader` returned ok and `listStatisticsReaders` confirmed the exact supplied principal as the sole active reader. No player records or gameplay counters were changed. The owner confirmed on 2026-09-06 that the published panel works after signing in with the granted account. This is manual owner confirmation of real authenticated production viewing.

## Explicit crowded-item selection — local, 2026-09-06

Fixed the reported case where a loaded plank consumed E before nearby loose canisters. Expeditions now use one per-player target list for the prompt, exact-object corner outline/name and action. Available cargo takes default priority; X or the touch Next target button cycles other items and bus/socket/recovery actions. Blocked supports remain selectable with an amber label and the existing reason, but sort after available actions. Cargo no longer occludes another loose module's handle, and teammates do not block the pickup ray. Solid terrain, the chassis and support geometry still block reaching through them. Loaded-support and occupied-hand checks remain active. The empty socket is no longer highlighted when targeting its loose module.

Reliable interact actions carry the displayed target ID, which the host revalidates. A stale/contested target is rejected rather than selecting another item or accidentally placing a just-picked prop. Each player retains independent selection; cycling cancels a held removal/kick. Snapshot metadata is validated and the compatibility suffix advances to net3 so older peers cannot silently use different interaction rules. Room and records APIs/storage, map geometry and cargo tuning are unchanged.

Verified: 51 focused headless cases passed across target selection, existing canisters/field kit, multiplayer ownership/protocol and physical winch recovery. Eight new target cases cover the crowded pile, deliberate blocked-support selection, either canister, adjacent cargo visibility versus solid chassis occlusion, stale actions, competing players, held removal cancellation and malformed packets. Existing loaded-support tests now deliberately select the blocked support, since a nearby free action has priority. Strict TypeScript and production build passed with the existing bundle/worker warnings. One Chrome scenario passed in 1.1 minutes using the real expedition with only obstacle positions arranged by a test fixture: keyboard and touch switching, exact labels, blocked support feedback, actual canister pickups and pause visibility. Desktop and touch screenshots were inspected. The initial browser launch was sandbox-blocked from binding localhost; the permitted rerun passed. Documentation validation passed with 14 entrypoints.

Ready for local playtesting at http://127.0.0.1:5173/?mode=expedition. No production deployment or profile/record writes in this task. Both local teammates need the updated frontend and a fresh invite. Two-player selection/ownership is automatically tested in the authoritative simulation; a fresh end-to-end WebRTC session and separate-network play were not rerun for this change.

## Expedition release 0.7.0 — published, 2026-09-06

The user requested deployment after local playtesting. Published the extended Forest crossing (content version 3), 6.8 m planks, recovery trees, simplified Solo/Co-op entry, shared audio/account header and account-bound rooms. Only `frontend rooms` were targeted: the original frontend upgraded, and rooms was created as `zsua7-paaaa-aaabz-aadfq-cai` on nano-tema. Thirteen assets synced. Frontend and rooms are Running with 0.7.0 metadata and the correct sibling IDs. The original records ID, WASM, memory and complete settings match the pre-deploy readback; its implementation and II origin files retain their previous hashes. No records deployment/reinstall or test profile/result writes occurred.

Verified: all 152 headless tests and five actual-Wasm room access/retry/upgrade cases; frontend/rooms build with strict TypeScript; public read-only exact assets/illustration, headers, identity origin, canister wiring, records 0.6.0/private-data rejection and boards. Published Chrome guest expedition startup, walking input and pause/resume passed with no page errors, development getters or canister update calls; menu/game screenshots were inspected. The public smoke scripts now check rooms wiring and the expedition entry without adding production activity or accounts. Documentation validation passed with 14 entrypoints. Release hashes, IDs and repeatable commands are in [Deployment](./deployment.md).

The deployment identity remained valid, so no further login was needed. This publishes the current direct-connection implementation; TURN is still unconfigured, and a real two-account session between different networks has not been demonstrated. The user was informed before deployment. Room/profile telemetry and character outfits remain planned. Local development remains available, and both players should reload the published game before creating a new invitation.

## Extended Forest crossing with recovery trees — 2026-09-06

The user confirmed the longer planks work, then requested a longer map with stones, a hollow and a climb on every approach. Implemented locally in content version 3: a 64 × 192 m heightfield (previously 64 × 104), the finish at Z=170 instead of 82, and staggered left/centre/right stone clusters, hollows and uphill sections. Terrain through Z=72 and the working bridge remain intact. The map contains five planks, twelve movable stones, twelve fixed boulders and thirty marked winch trees. Paths stay connected and meet on the production plateau. [Maps](./maps.md) describes the terrain and resource layout.

Automatically verified: 30 focused map/field-kit/protocol/physical-route/recovery tests passed. They demonstrate a complete delivery by each approach with the relevant stones moved and cargo intact, including the centre bridge and the left winch climb. Six recovery cases were then strengthened and passed again with actual walking of the carried cable: each new hollow, the original left hill, and both near-bank central-gap trees. The gap cases physically drive off the unbridged edge, then retrieve the upright bus with all modules back onto its departure bank. The hill's upper tree was moved past the crest after a test exposed the minimum-cable-length stop. Far-bank and lateral gap candidates respectively wedged or tipped the bus; the final near-bank locations passed both directions. Early route fixture failures also showed that the existing left starting stones must be cleared before taking that approach.

The final Chrome visual inspection passed in 18.5 seconds with no page errors. It renders the real map with a survey camera; overview, bridge/recovery trees and all three extension sections were inspected. This is visual evidence, not a browser-driven or human full-map playthrough. The first visual pass preceded the final gap-tree adjustment; only the affected map inspection was repeated. Strict TypeScript and production build passed with existing worker/chunk warnings; map validation passed (65 × 193 samples, 17 resources). Documentation validation passed with 14 entrypoints.

The new map is ready for local playtesting at http://127.0.0.1:5173/?mode=expedition. Reload and start a new expedition; both teammates need the new version and a fresh invitation. The 5–10 minute card remains an editorial estimate. Human route balance, arbitrary falls/rollovers and new two-person playthroughs remain unverified; inverted vehicles or ejected winch modules still require the existing righting/reinstallation actions. No backend/profile/solo-rule changes or production deployment were made. A copy of the previous map is retained locally at `/private/tmp/forest-crossing-v2.json`.

## Longer planks and bridge placement — 2026-09-06

Implemented locally: plank length increases from 5.4 to 6.8 m, preserving its width/thickness and the shared visual/collision mesh. The old 5.08 m support span left only 0.08 m beyond the central gap's 5 m bank separation; the new span is 6.48 m. All support, slope, burial, obstruction and loaded-prop checks remain active. Invalid plank slope now asks players to rest both ends on banks or stones. The two starting planks move one metre forward so longer ends do not intercept passenger-door interaction; Forest crossing content version is now 2, with unchanged terrain. Map compatibility now also hashes field-kit tuning to reject mismatched peer geometry.

Automatically verified: 22 focused physical/map/protocol tests passed, including actual placements at 18 combinations of two wheel tracks, a one-metre bank approach and yaw offsets up to ±0.12 radians; real wheel support; the bus crossing on two planks versus failing on the bare gap; dependent stone/load protection; the right-side delivery; driver/passenger swapping; and map/tuning fingerprint compatibility. The first crossing fixture selected a nearby spare plank after lengthening; its approach was moved clear of supplies. A separate passenger-seat regression exposed the starting-pile proximity and prompted the map correction. Strict TypeScript and production build passed with existing worker/chunk warnings.

The focused automated Chrome check passed on the final map in 47 seconds: start unsigned exploration, pick up a plank, carry it from the supplies to the ravine using ordinary keys, see a valid preview and place it from the bank. Preview/placed screenshots were inspected; no page errors. The earlier check also passed before moving the starting pile; the final run covers that map correction. Map validation passed for content version 2 (65 × 105 samples, 11 resources). Documentation validation passed with 14 entrypoints.

No production deployment or profile/records persistence changes. Both teammates must reload the local build and create a fresh invitation after this map/tuning change. Arbitrary positions/angles still require valid supports; human assessment of the new placement feel remains pending.

## Title layout, route card and shared header — 2026-09-06

Implemented locally from the user's reference: Solo / Co-op navigation sits above each title, with the active mode highlighted. The old Next Up Multiplayer teaser, Solo deliveries label and Solo for now note are removed. Co-op uses DELIVER TOGETHER, a prominent Create expedition button and an always-visible invite form. A compact upper-right Forest Crossing card uses bundled abstract forest artwork, 5–10 min, Moderate and 3 paths; on narrow screens it stacks before the launch actions. The time is an editorial estimate. Card metadata/art are separate from map JSON, which retains its terrain, resources and network identity.

Both modes use shared header markup with independent Music and Sound toggles and Internet Identity status (Sign in, Connecting…, nickname). All three controls remain visible on narrow screens. Co-op header sign-in opens the existing garage without making a room, while invite sign-in continues its pending join. Garage drafts apply before joining; account editing is disabled during an active team session so its agreed profile stays stable. The redundant menu account/sign-out row is replaced by the header and garage.

Verified: production build and strict TypeScript passed, with only existing worker/chunk warnings. The final focused browser check passed in 25.5 seconds: navigation/active mode, unchanged three solo choices, compact route card, visible invite validation, independent audio toggles, cancelled header login feedback, connecting/signed-in/garage/sign-out states, and return to solo. Real local profile calls were signed; only the human II gesture was substituted. Desktop and 390 px screenshots were inspected; no page errors or horizontal overflow. The earlier shared-header iteration also passed its focused check; the later reference changed navigation/presentation, so only the affected entry check was repeated. A touch layout inspection identified cramped card/action spacing in that earlier version; the final narrow layout uses stacked flex items with explicit gaps. Existing invite/review selectors were updated, without replaying their complete physics runs. Documentation validation passed with 14 entrypoints.

No gameplay, map data, profile storage, room protocol or production deployment changed in this UI task. The local preview remains available at http://127.0.0.1:5173/?mode=expedition. Real human-II and external-network acceptance limits remain as documented below.

## Account-bound multiplayer and measurement plan — 2026-09-06

Implemented locally: two-player entry now restores/requests the existing II account and resumes the same create/join intent. Cancelled sign-in retains the invitation. Solo exploration remains unsigned. Room calls reuse the account identity; guest/host must use distinct accounts. The lobby shows saved nicknames and both peers use the host's saved bus appearance. Protocol 2 bounds this presentation to the existing catalogs. Failed self-join does not close the host's room; a returning host's new invitation replaces only that host's prior invitation. The records actor/interface, auth options and permanent II origin file still match their pre-expedition hashes.

The [accounts and measurement plan](./multiplayer-accounts.md) defines a small future outfit catalog and a room → join → connection → start → finish funnel, failure stages, guest-to-host/repeat-team metrics, and bounded controller-only host/guest diagnostics. Character outfit selection and historical multiplayer telemetry are **planned, not implemented or collecting**. Existing short-lived room membership is not a historical analytics log.

Verified: production build/strict TypeScript; 24 focused account/protocol/simulation cases; five actual-Wasm rooms cases for anonymous/outsider access, ownership, exact retries, upgrade, expiry and own-invite replacement. A separate upgrade from the archived pre-change rooms Wasm retained the existing host, guest, offer and answer. The representative browser flow passed in 1.9 minutes against local records/rooms: unsigned exploration, required host login, rejected same-account join without deleting the invitation, cancelled guest login/retry preserving its room, two distinct accounts and nicknames, guest Ready/host Start, replicated marker, shared pause/leave, saved account restore, and invalid/manual invites. Only the human II gesture was substituted; signed calls used real local canisters. No page errors. An earlier run hit the test's five-second third-context loading timeout; its rendered login screen and trace showed no application error, and the successful run used a longer initial-load wait. Login/lobby/390 px menu screenshots were inspected. Documentation validation passed with 14 entrypoints.

The unchanged records canister was installed on the disposable local network and the local rooms canister upgraded. Vite restarted with both services at http://127.0.0.1:5173/?mode=expedition. Local profiles are separate from production. No production deploy or writes occurred. Manual human-II/local two-account playtesting, separate-network/TURN verification and full human cooperative play remain outstanding; no additional full-track browser replay was needed for these entry changes.

## Start screen and invite correction — 2026-09-06

The user's two-window screenshot showed two host lobbies under different invitation URLs. The old invite route prefilled a small Join field while retaining the large Create button. Opening a link now enters the guest connection directly; host creation and leaving remove stale room URL parameters. Host/guest labels and matching room IDs make ownership visible. Only the host sees the copy action, and sharing controls disappear after connection. The guest chooses Ready; the host starts with one click. A ready message arriving before the final local ICP reply is retained after hello validation.

The single-map selector is hidden, manual joining lives under Have an invite?, and duplicated menu controls/help text were reduced. The overview and world signs use Local build → Production; the finish uses deployment-themed copy. The map catalog, terrain, gameplay rules, rooms actor and records/profile persistence are unchanged.

Verified: production build and strict TypeScript passed. The focused real-ICP/two-browser regression passed in 1.3 minutes: opening the link automatically joins the same room, host/guest roles differ, guest Ready unlocks one host Start, a guest marker reaches the host, pause/leave work, room URLs clear, malformed invites never show Create, and manual form submission works with Enter. Desktop menu/host/guest and 390 px menu screenshots were inspected; no browser page errors occurred. Established physics/backend checks were reused because those implementations did not change. The full expedition regression was updated for the new entry flow without replaying its already-verified driving sequence. The local preview remains on port 5173; no production redeployment occurred.

## Local expedition implementation — not published

Forest crossing is an authored 64 × 104 m clearing with a central gap, steep left hillside, longer right approach, five planks, six stones, marked winch anchors and physical boulders. A bounded version-1 JSON schema and automatic catalog separate maps from code. Template creation, refusal to overwrite and map validation have been exercised; the temporary template was removed. Map uploads/editor and online user-map storage remain future work.

Planks/stones have stable IDs, matching visual/physical geometry, placement previews and support checks; loaded supports cannot be removed. Players can push and mark points. Existing solo maps retain their rule identities. The DOM-free simulation now accepts two owned players, exclusive driver/passenger seats, per-player cargo/field-kit ownership and an owned cable/operator. The expedition UI adds create/join invites, both-ready/host-start, shared pause and host restart. WebRTC carries gameplay directly with action deduplication, input expiry, complete snapshots and bounded interpolation.

The new rooms actor is independent of records. It was built and installed only on a disposable local ICP network. Four actual-Wasm tests passed for anonymous/outsider rejection, two-seat ownership, immutable/idempotent SDP, upgrade preservation, expiry and member cancellation. That initial iteration used a short-lived browser identity; the later account-bound entry described above supersedes it. TURN credential endpoint configuration is supported; no TURN service/provider is configured yet.

Verification so far: strict production build passed (existing large-chunk/worker warnings only); the complete 137-case headless suite passed, followed by focused tests for independent helper winching, stone-supported ramps and the alternate right-side delivery (140 cases total). Physical tests show a real bus crossing the central gap on two placed planks and being blocked without them. Another test proves a stone supporting a ramp cannot be removed. The full right-side delivery passed after moving its three loose stones; it uses no planks or central bridge. Initial scripted attempts were blocked by obstacles/uncleared stones, so this is evidence for that cleared route, not every possible path. The representative two-browser integration passed in 1.3 minutes: actual local ICP invitation, both-ready/start, shared carried/placed plank and marker, guest-controlled driving, guest pause/resume, host restart, and visible disconnection. It also asserted that no ICP requests were made during gameplay. Desktop host/guest screenshots were inspected. An earlier HUD sizing issue was fixed; a test now waits for the authoritative boarding prompt, and an edit-triggered Vite reload was excluded from the final stable run. A final visual check found inward prop faces; triangle winding was corrected, all eight field-kit physical tests passed again, and local exploration plus the narrow menu were rendered and inspected. No claim of a complete human cooperative playthrough is made. The local Vite game and local rooms network are running for the user at http://127.0.0.1:5173/?mode=expedition. The user requested trying this before redeployment; production remains unchanged.

Preservation: SHA-256 checks match the pre-change records Motoko source, Candid interface, auth options and II alternative-origin file. A pre-change source/config/docs archive is available locally. No production writes, installation or deployment were performed during this task. All existing profiles, leaderboard data and record history remain in their original canister.

## Published implementation

- Three solo routes, keyboard/touch controls, removable capability canisters, terrain-supported winch, optional rollover rocks and physical kick recovery.
- Optional Internet Identity with a shared account origin across the custom and original canister domains. Nickname, cosmetic garage, private times and opt-in current-course top-20 boards persist in the backend.
- Guest finishes can be claimed after II without another drive; explicit Join enables publication. Signed autosaves preserve sharing preferences. Retry/cancellation retains the displayed run. Unsent results remain tab-memory-only.
- Controller-only counters track openings and per-course starts, finishes and first saves. `npm run stats:cloud` returns readable fields. Counters start with 0.6.0; they are approximate events, not unique people or historical traffic.
- Static social preview metadata/image, finish credits and the OpenCloud link are published.

## Latest verified release — 0.6.0

Strict TypeScript, 121 headless/account tests and 20 actual-Wasm backend checks passed, including a real 0.5.3 upgrade, caller isolation, idempotency, controller rotation and retained data. One complete local browser scenario passed in 2.5 minutes: physical guest delivery, cancelled II, network failure, save retry, leaderboard, reload and exact counters/practice exclusion. Only the human II gesture was substituted. Desktop and portrait finish screenshots were inspected; final heading polish used a separately labelled DOM-only fixture.

Both canisters upgraded successfully. Public read-only checks passed release assets/headers, shared II origin, API privacy and boards, and social metadata/image. No production test event/profile/result was written. A records-only Candid correction changed custom metadata with byte-identical executable sections; the live controller query kept its start timestamp and one opening across that upgrade. Detailed hashes and evidence remain linked below.

## Remaining limits

The user confirmed live II sign-in; real human-II finish/save/reload and physical-phone coverage are still separate manual checks. Browser-reported times and public event writes are not anti-cheat certified. Arbitrary-obstacle cable wrapping, TURN service provisioning, public expedition deployment, real-device separate-network verification, measured 100 ms RTT / 1% loss and human cooperative playtesting remain outstanding. Local two-browser results do not establish those gates. Guest movement currently has no prediction; its added interpolation delay needs playtesting.

## Workflow update — proportional verification

The user requested fewer repeated browser checks and lower token overhead. AGENTS.md now defaults to one representative scenario for a new integration and focused later checks. Copy/styles do not trigger complete driving; backend ownership/retry/upgrade tests remain when persistence changes. Already-passed checks are reused until a relevant edit or failure invalidates them. Builds/checks and releases are batched, outputs are bounded, and unrelated history is read only on demand.

That earlier workflow-only change modified documentation/rules without code, tests or deployment. The previous full status was preserved in the history document; current status is now a short entrypoint. Documentation validation passed: 11 entrypoints and their local links/hierarchy. No browser, game build or canister deployment was needed.

## Detailed verification history

Earlier changes, complete release evidence and diagnostic attempts remain available when needed; this is not required reading for routine edits.
Source: [Verification history through 0.6.0](./status-history.md)
